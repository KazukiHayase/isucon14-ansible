all:
  vars:
    ansible_host: localhost
    ansible_user: root
    ansible_ssh_private_key_file: ./.ssh_local/ansible_local_key
    ansible_port: 2222
  hosts:
    server1:
      use_mysql: true
      use_nginx: true
      use_app: true
    server2:
      use_mysql: true
      use_nginx: true
      use_app: true
    server3:
      use_mysql: true
      use_nginx: true
      use_app: true
